# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# dist/ IS committed: Flask (controllers/pages_controller.py) serves this
# build output directly at "/". There's no separate build/deploy step for
# this locally-run app, so the built bundle has to live in git — run
# `npm run build` here and commit the result whenever src/ changes.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
