# Версії запіновані навмисно: без цього CI щоразу тягне найсвіжіший апстрім,
# і білд може почервоніти від чужої зміни, а не від коду в цьому репозиторії.
# Оновлення — свідома дія: `pip install -U <пакет>`, прогнати pytest, вписати
# сюди нову версію.
flask==3.1.3
openai==2.46.0
beautifulsoup4==4.15.0
pillow==12.3.0
numpy==2.4.6
scipy==1.18.0

# --- тільки для розробки (tests/) ---
# Застосунку не потрібен: `python3 app.py` стартує і без нього.
pytest==9.1.1
